# 赛博鱼乐签到缺口处理进度

处理时间：2026-09-19（Asia/Shanghai）
范围：Android APP 签到、Fastify/Prisma 签到服务、Web 管理端签到设置。

## 本轮已完成

- 奖励规则按周期策略计算，签到记录与 `CheckInRewardLog` 在同一 Prisma 事务中写入；奖励日志使用用户、日期、奖励日唯一约束保证幂等。
- 签到请求支持稳定 `deviceId`；成功签到记录保存设备标识，IP 频控、账号设备数和同设备多账号检测写入 `CheckInRiskEvent`。
- 设备超限只记录风控事件，不阻断正常签到；同设备多账号按不同账号去重。
- IP 频控只统计 `CHECK_IN_ATTEMPT`，风控记录写入失败不会阻断签到主流程。
- 新增管理员风控事件分页接口，默认查询近 7 天；页面显示时间、原因、账号、设备、IP，并可展开查看事件上下文。
- 签到配置增加每日时间、活动时间、奖励周期上限和预置图标校验；奖励页使用预置图标选择器。
- 签到配置审计模块使用 `CHECKIN_SETTINGS`，保存、发布、回滚带 before/after 或变更快照；`REVIEWER` 不再拥有发布权限。
- 重复签到使用 `CHECKIN_ALREADY_CHECKED_IN`（40912）；APP 解析错误码并刷新签到概览，避免重复点击后仍显示未签到。
- APP 签到结果显示服务端返回的奖励；签到历史请求统一携带当前月份。
- 增加 APP `CheckInScreen` Compose 测试和重复签到错误码解析测试。
- 增加服务端隔离数据库回归测试，覆盖奖励幂等、IP 限频、设备超限不阻断、配置非法值和并发重复签到。
- 新增签到运营统计接口与管理端近 7 日统计卡片/明细表；生产统计接口返回 7 天分桶及总计。
- 新增历史设备 sentinel 回填迁移 `20260920_check_in_legacy_device_backfill`；生产核验 `nullCount=0`、`legacyCount=3`，sentinel 不参与真实设备风控计数。
- APP 新增按用户/月隔离的签到缓存，概览缓存 TTL 为 5 分钟，网络失败回退缓存并显示重试入口；缓存策略单测和 Compose 离线提示测试通过。
- 真机 `MEIZU Note 16 Pro` 已完成正式签名 APK 覆盖安装；发布签名 instrumentation 的签到测试 2/2 通过。断网刷新显示“网络不可用，当前显示最近同步的签到数据”，恢复网络后重试显示“今日已签到，明天见”。

## 本轮验证

- `cyberfish-admin/server`: `npm run typecheck` 通过。
- `cyberfish-admin/server`: `npm run build` 通过。
- `cyberfish-admin/server`: `npx prisma migrate status` 显示数据库 schema up to date。
- `cyberfish-admin/server`: `npm run test:check-in` 通过，7/7 项签到回归测试通过。
- `cyberfish-admin/web`: `npm run typecheck` 通过。
- `cyberfish-admin/web`: `npm run build` 通过；仅有既有 bundle 体积和动态导入提示。
- `app`: `:app:testDebugUnitTest`、`:app:compileDebugKotlin`、`:app:compileDebugAndroidTestKotlin`、发布签名 APK 构建通过。
- 根目录：`git diff --check` 通过。
- 真机 Debug connected 任务不能直接使用 Debug 主包匹配发布签名；改用同一发布 keystore 重签 androidTest APK 后，真机签到 Compose 测试 2/2 通过。
- 本地提交为本轮收口提交；GitHub `main` 已通过 Git Data API 同步，远端 tree 与本地 tree 完全一致。
- 生产数据库已备份到 `/opt/cyberfish-admin/shared/backups/45a150f-before-migrate-20260919T083818Z.db`，7 个迁移均已应用，`prisma migrate status` 显示 schema up to date。
- 生产 API release 已切换为 `/opt/cyberfish-admin/releases/45a150f/cyberfish-admin`，`cyberfish-admin.service` 为 active，本机和公网健康接口均正常。
- 线上 `sakura` 登录、签到概览、按月历史通过；重复签到返回 HTTP 409、业务码 40912，并携带当日记录和概览；管理员风控列表与签到奖励配置接口返回 200。
- 服务端签到回归测试已接入 GitHub Actions；Web 风控事件支持页面上的日期范围和事件类型筛选，并已重新构建部署到 `/var/www/lycwd.com`。部署前静态站点备份为 `/opt/cyberfish-admin/shared/backups/web-before-10d09c1-20260919T080100Z.tar.gz`，首页与健康接口返回 200。
- 生产统计接口、历史设备回填和 Web 统计页已随 `45a150f` 部署；线上统计返回 7 天分桶，sakura 概览/历史仍通过。

## 仍未完成的签到相关缺口

1. 服务端回归测试已接入 CI；本轮代码触发的 Cyberfish CI #33（run `35433136404`）已完成 success，verify job `105871139681` 和 web job `105871139779` 均 success。
2. 风控事件列表和近 7 日运营统计已完成；更长期趋势/留存类看板不在本轮签到缺口范围。
3. 历史设备标识已使用明确的 `__legacy_unknown__` sentinel 回填，无法恢复的真实设备信息不会伪造为具体设备。
4. APP 离线缓存、5 分钟刷新策略和真实设备 Compose/HTTP 联合验收已完成。

## 项目其他发布缺口

- 真实鱼漂模型性能门禁、长期共享 APP Token、强制更新阻断、误报视频闭环、模型 OTA 自动安装和生产部署证据仍未闭环。
- 低网、低存储、安装取消、OTA 中断、签名失败、模型回滚和真实上鱼声音验收仍未完成。

## 当前工作树说明

- 本轮签到改动已提交、同步远端并部署生产；本地与远端 commit SHA 因 Git Data API 同步方式不同，但 tree 完全一致。当前最后本地提交为本轮收口提交，生产后端对应 `45a150f`，Web 对应当前构建，正式签名 APK 已在真机验收。
- `notes/`、`sdk/`、`训练资源/`、服务器归档压缩包及历史上下文文件为已有未跟踪内容，本轮未纳入。
